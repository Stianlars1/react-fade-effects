import React from "react";
import { FadeWordsProps } from "../../types/types";
export declare const FadedWords: ({ words, className, filter, duration, staggerTime, delay, variant, scaleSize, once, translateAmount, }: FadeWordsProps) => React.JSX.Element;
